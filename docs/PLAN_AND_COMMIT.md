变更计划与建议提交信息

概述
- 本次变更聚焦于：
  - 行政区缓存规范化（合并 "河南" / "河南省" 等重复键）并写回 `config/region_cache.json`。
  - 禁止程序启动时自动联网拉取行政区；仅在用户点击“更新行政区”时联网拉取指定省份并合并到缓存。
  - 将天地图、百度、高德等 provider 的调用逻辑整理到 `providers.py`，改进天地图 v2/search 的使用优先级与回退。
  - GUI 在刷新行政区时读取并使用规范化后的缓存（避免重复/缺失显示）。
  - 为核心函数添加中文注释，新增调试/验证脚本以便本地回归测试。

已完成的工作（简要）
- 新增/更新函数：`unify_region_cache`（合并规范化缓存）、`fetch_and_save_region_hierarchy(..., target_provinces=...)`（按省合并更新）、`ensure_region_data`（启动时不联网，优先使用缓存/内置数据）。
- GUI：`gui_pyqt.py` 在刷新时调用 `unify_region_cache` 并从缓存构建区域树。
- providers：重构 provider 调用位置，并实现天地图优先策略。
- docs：本文件 + `docs/USAGE.md`（使用说明）已创建。
- 为关键函数加入中文注释（便于维护与代码审查）。

建议的 Git 提交信息（短）
chore(region): 规范化 region_cache、GUI 从缓存加载、延后首次联网更新，补充中文注释

建议的 Git 提交信息（详述）
- 规范化 `config/region_cache.json` 顶层省级键，合并重复命名（如“河南”/“河南省”）。
- GUI 刷新时使用规范化缓存并在用户点击“更新行政区”时按指定省份合并网络拉取结果（不在启动时联网）。
- 调整 provider 调用逻辑与天地图优先回退策略。
- 为核心函数添加中文说明注释，并增加本地验证工具脚本。

后续计划（建议）
1. 完成对天地图 v2/search 在真实 key 下的回归测试，并调整超时/重试策略。
2. 在 GUI 中加入“仅合并选中省份”功能的可视化提示与进度显示。
3. 将规范化工具纳入 CI 检查，以防止后续手工编辑导致键名分裂。

备注
- 本地提交时请先运行 `python -m py_compile map_poi_fetcher.py` 或项目测试套件，确保无语法/导入错误。