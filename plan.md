### 本次版本

- 版本号：v1.0.0（初始完整发布）
- 说明：包含增量去重快照修复、多行政区展开修复、GUI 全局设置调整与资源码回写功能。
- [ ] 检查 “更新行政区” 功能，点击后能够将京津冀、陕西、山西、河南、湖北 区域的省、市、区县数据保存在本地的region_cache.json文件，只需要保存名字；
- [ ] 在其他情况下仅读取本地的region_cache.json文件，而不自动从网上获取补全；
- [ ] 检查任务保存功能：特别保证GUI界面选择的区域和资源保存到了poi_config.json 文件中；
- [ ] 检查任务解析GUI展示功能：能够正确解析任务，并正确显示在GUI界面上，即保证任务的保存和解析是一致的。
- [ ] 检查任务解析后，能够自动展开以选择的相应树节点。
- [ ] 点击保存任务时，同时保存全局设置(导出格式)。
- [ ] 检查任务解析执行功能：保证POI请求时最大行政区域为区/县，资源点也是逐个查询，避免请求的数据数量过大，无法获取全量数据。
- [ ] 首先检查天地图查询是否成功 下面时天地图的请求API，http://api.tianditu.gov.cn/v2/search?postStr={"queryType":13,"start":0,"count":5,"specify":"156110000","dataTypes":"法院,公园"}&type=query&tk=您的KEY
- [ ] | 参数值    | 参数说明                                                                     | 参数类型 | 是否必备 | 备注（值域）                                                              |

  | --------- | ---------------------------------------------------------------------------- | -------- | -------- | ------------------------------------------------------------------------- |

  | specify   | 指定行政区的国标码（行政区划编码表）严格按照行政区划编码表中的（名称，gb码） | String   | 必填     | 下载行政区划编码表。9位国标码，如：北京：156110000。                      |

  | mapBound  | 查询的地图范围(“minx,miny,maxx,maxy”)                                        | String   | 必填     | -180,-90至180,90。                                                        |

  | queryType | 服务查询类型参数                                                             | String   | 必填     | 13:分类搜索服务。                                                         |

  | start     | 返回结果起始位（用于分页和缓存）默认0                                        | String   | 必填     | 0-300，表示返回结果的起始位置。                                           |

  | count     | 返回的结果数量（用于分页和缓存）                                             | String   | 必填     | 1-300，返回结果的条数。                                                   |

  | dataTypes | 数据分类（分类编码表）                                                       | String   | 可选     | 下载分类编码表，参数可以分类名称或分类编码。多个分类用","隔开(英文逗号)。 |

  | show      | 返回poi结果信息类别                                                          | String   | 可选     | 取值为1，则返回基本poi信息； 取值为2，则返回详细poi信息                   |
- [ ] 检查手动执行任务时是否正常，是否覆盖了配置内容，是否存在未获取到全量数据，先以河北省的加油站和医院进行测试，可以先以一个县->一个市->一个省，构建三个任
- [ ] 然后测试定时任务，以一个区(医院)进行定时执行(一次/2min)，实际应用中定时计划是以天为单位。
- [ ] 保证天地图没有问题后，开始测试高德地图，同样的流程。

## 2026-04-20 变更记录

- 修复：最终去重逻辑现在使用任务开始时的增量键快照，避免本次运行期间已追加到增量文件的数据被误判为“已存在”从而导致内存结果被全部去重（参见 map_poi_fetcher.py）。
- 修复：修正多行政区展开问题，确保选择多个区/县时会为每个行政区生成并执行子任务。
- 改进：`gui_pyqt.py` 中移除“全局设置”选项卡；在保存任务时同时保存导出格式 `export_format` 到全局配置，并在保存时把 `gaode`/`tianditu` 的资源码转回中文名称再写入任务配置。

测试/验证建议：

- 在 GUI 中保存任务（包含天地图或高德的资源代码），检查 `config/poi_config.json` 中 `tasks[].resources` 是否为中文名称且 `export_format` 已更新。
- 运行一个真实任务并观察日志最后一行的“抓取总数 / 去重后 / 输出”是否合理。

